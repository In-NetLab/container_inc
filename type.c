#include "exchange.h"
#include "type.h"

#include <assert.h>

// not implemented
struct PackInfo from_pack(pack_t pack) {
    struct PackInfo nothing;
    assert(0);
    return nothing;
}

void send_pack(struct PackInfo pack_data) {
    // not implemented
    assert(0);
}